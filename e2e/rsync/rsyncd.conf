uid = root
gid = root
use chroot = no
max connections = 10
log file = /dev/stdout
pid file = /var/run/rsyncd.pid

[data]
path = /data
comment = djmgmt E2E test data
read only = no
auth users = testuser
secrets file = /etc/rsyncd.secrets
hosts allow = *
