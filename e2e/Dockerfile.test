FROM python:3.11-slim

RUN apt-get update && apt-get install -y rsync ffmpeg && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY pyproject.toml .
COPY src/ src/
RUN pip install -e .

WORKDIR /e2e
CMD ["python", "-m", "unittest", "test_integration", "-v"]
